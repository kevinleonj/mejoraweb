# docs/

Internal documentation for the mejoraweb project.

This folder is tracked in git but is **never served or accessible from the deployed app** — Vite only processes files in `src/` and `public/`. Everything here is purely internal.

## Structure

```
docs/
  improvements/    # Feature plans, improvement proposals
  audits/          # Web performance, SEO, accessibility audits
```
