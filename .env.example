# Contact form — Cloudflare Worker URL
# Set up a separate worker for mejoraweb (isolated from limeralda's worker for independent metrics).
# The worker should accept POST requests with JSON body { name, email, message }
# and forward emails to: info@limeralda.com, andres@limeralda.com, kevin@limeralda.com
VITE_CONTACT_WORKER_URL=
